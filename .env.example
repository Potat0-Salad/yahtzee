# Consumed by docker-compose.yml for the Postgres container.
# Mirror these values into backend/.env so Django connects to the same database.
POSTGRES_DB=yahtzee
POSTGRES_USER=yahtzee
POSTGRES_PASSWORD=yahtzee
